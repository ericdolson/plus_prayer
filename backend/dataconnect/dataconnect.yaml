specVersion: "v1"
serviceId: "plusprayer-service"
location: "us-central1"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "plusprayer-database"
        cloudSql:
          instanceId: "plusprayer-instance"
          # schemaValidation: "STRICT"     # Postgres schema must match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # Postgres schema may contain extra objects (friendlier while iterating).
connectorDirs: ["./connector"]
